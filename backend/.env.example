# Hanzla Outlet API – environment template
# Copy to .env and fill in real values. Never commit .env.

# Async PostgreSQL URL (SQLAlchemy + asyncpg driver)
# If password contains @ or #, URL-encode: @ → %40, # → %23
DATABASE_URL=postgresql://postgres:Armin@11Armin@db.adcqdrunyhpvcclmlddd.supabase.co:5432/postgres

# JWT / session secret – use a long random string in production
SECRET=b0e2f1879da4356c

# Allowed CORS origins (comma-separated; frontend dev server)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
